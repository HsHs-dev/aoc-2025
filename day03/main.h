#include <stdint.h>

// find the two largest char numbers and return the resulting number from concatinating them
int find_two_largest(char* line);

// concatenate x and y into one number
uint64_t concatenate(uint64_t x, uint64_t y);

// solves the problem in part1 in a single pass
int find_two_largest_linear(char* line);

// find largest 12-digit number that can be made from an X digit number
uint64_t find_largest(char* line);